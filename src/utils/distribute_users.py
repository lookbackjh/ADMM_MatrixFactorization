



def distribute_users(users,num_users, num_block) :
    # user has  user_id, item_id
    # num_block is the number of blocks to divide the users into 
    # num_users is the total number of users

    each_block = num_users//num_block
